{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run OpenRecon",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "console": "integratedTerminal",
      "args": ["example.com", "--output", "results.json"]
    }
  ]
}